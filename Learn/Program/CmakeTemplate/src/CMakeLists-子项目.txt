CMAKE_MINIMUM_REQUIRED(VERSION 2.6) #版本号
INCLUDE_DIRECTORIES(../include)#头文件
AUX_SOURCE_DIRECTORY(. SRC_LIST)# 添加目录源文件 加入变量SRC_LIST

ADD_EXECUTABLE(funcA ${SRC_LIST})#编译

set (EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin) #设置输出
