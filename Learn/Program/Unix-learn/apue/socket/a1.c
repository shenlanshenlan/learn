//创建套接字  然后建立连接
//       client


#include "include.h"
struct  sockaddr_in  *cadd  ,*cadd;




int main() 
{ 
       											//地址

			fd	=	 socket(AF_INET,*cadd,0) //创建套接字
	  
		  connect(fd,sadd,len);           // 建立连接
	
	   	read(fd,str,size);			  			// 读取内容
