// 数据链路层控制  使用root 账户

#include "main.h"
#include "fun.c"

//主函数
int main(int argc, char *argv[])
{  
    int c, lopt = 0; //
    int zerosum;
    char *ptr, localname[1024], *localport;
    struct addrinfo *aip;
    struct sockaddr  k_addr;
    struct sockaddr_in  *sss;
    char *device;
 
    open_pcap();

  
    get_pcap();
    
    

 
     OK;
 }
 
